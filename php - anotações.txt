$marca
$modelo
$ano
|
|
3 variáveis --> forma bolo --> Classe --> objeto(carro)

Atributos 
public -->São atributos públicos que podem ser acessados de para de uma classe 
private --> São atributos que acessados somente por dentro da classe
getter --> método padrão para ler ou obter valores de um atributo privado.
setter --> método padrão para alterar ou modificar valores de um atributo privado

ucworlds --> Deixa toda as inciais Maiúsculas

strlower --> Deixa todo o texto minúsculo

preg_replace --> altera a estrutura de uma string
pattern: /\D/--> significa qualquer caracter que não seja um digito

(int) $variavel garante valor inteiro

abs(variável) garante o numero positivo 



Array Associativo 

Diferentemente dos arrays convencionais que associam os valores armazenados no array a um índice, nos arrays associativos os valores são associados a uma chave, aonde as chaves assumem o papel do índice do array. Exemplo: 

$alunos["nome"] = "Gabrielson";

neste exemplo o valor "Gabrielson" está associado á chave "nome". 
Isso é feito para melhorar o entendimento do código e na execução e manipulação do array. Caso fossemos trabalhar com um array tradicional, teremos que lembrar o índice que iria corresponder a chave "nome".

Formato do arquivo JSON 
$dao -> criarAluno(new aluno ("1","João","DEV"));
$dao -> criarAluno(new aluno ("2","Maria","mecânica"));

-- JSON de $alunos[];
[
    { "id":"1",
      "nome":"João",
      "Curso": "Dev"
    },
    { "id":2,
      "nome":"Mariana",
      "Curso" :"mecnica"
    }
];

O mdoelo MVC para criação de sistemas 
- O Que é o padrão MVC 
Este padrão é usado para o desenvolvimento limpo e organizado de aplicações 
m->representa Model: representa os dados e as regras de negócio. Exemplo: Aluno.php(CRUD) e AlunoDAO.php 
V-> View: Cuida da parte gráfica e da interface com o usuário, ou seja, o que aparece na tela. Exemplo: Index.html ou Index.php
C-> Controller: é o intermediário entre a interface e os dados e regras de negócio. Exemplo: AlunoController.php

